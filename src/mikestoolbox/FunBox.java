package mikestoolbox;

//@WookiePluginAnnotation(Name="MTB Fun Box", Description="A collection of fun commands", Author="DosMike", Build=1, Version="1.0")
public class FunBox {
	/*@WookiePluginFunction(Type.onPrepare)
	public void prepareToolbox() {
		BoxCommand.registerCommand("slap <Player>", "toolbox.fun.cmd.slap.base", (src, args)->{
			args.<Player>getOne("Player").ifPresent(player-> {
				player.setVelocity(Vector3d.createDirectionDeg(BoxLoader.RNG.nextDouble()*360, BoxLoader.RNG.nextDouble()*90));
				player.sendMessage(Text.of(src.getName() , " slapped you"));
				player.getLocation().getExtent().playSound(SoundTypes.ENTITY_PLAYER_HURT, player.getLocation().getPosition(), 1.0);
			});
			return CommandResult.success();
		});
	}
	@WookiePluginFunction(Type.onUnload) 
	public void destroyToolbox() {
		
	}*/
}
